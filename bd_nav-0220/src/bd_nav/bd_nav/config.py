# Distance scope (meters) for bd_nav. Change these to adjust behavior independently.
# - PROCESS_DIST_M: node listing, parsing, features, graph download (map_generator, path_generator, user_input, extractor)
# - RENDER_DIST_M: map tile extent / visualization only (map_viewer)
PROCESS_DIST_M = 400
RENDER_DIST_M = 300
